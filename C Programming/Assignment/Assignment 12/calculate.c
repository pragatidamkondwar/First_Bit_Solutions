#include<stdio.h>
#include<string.h>
void main()
{
	char str[50];
	int i, count=0;
	printf("enter the string");
	scanf("%s",str);
	int len=strlen(str);
	for(int i=0;str[i]!='\0';i++)
	{
		if(str[i]==' '&& str[i]=='\n'|| str[i]=='\t')
	}
}