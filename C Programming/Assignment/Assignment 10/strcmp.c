#include<stdio.h>
#include<string.h>
void main()
{
	char str1="abcd";
	char str2="abce";
	int cmp=strcmp(char "str1", char "str2");
	if(str1>str2)
	{
		printf("str1 is greater %s%s",str1,str2);
	}
	else if(str1<str2)
	{
		printf("str2 is less %s%s",str1,str2);
	}
	else
	{
		printf("both are equal%s",str1,str2);
	}
}